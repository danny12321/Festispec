<Window x:Class="Festispec.View.BaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Festispec.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        WindowState="Maximized"
        Title="Festispec CIB">


    <StackPanel Orientation="Horizontal">

        <Border Background="#32385D" Padding="0,30,0,0">
                 <ListBox FontSize="25" IsEnabled="{Binding IsChecked, ElementName=EnableListBox}" Background="#32385D">
                
                <TextBlock><Hyperlink Command="{Binding NavigateHomeCommand}" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="HomeVariant" VerticalAlignment="Center"/>Home</Hyperlink></TextBlock>
                <TextBlock><Hyperlink Command="{Binding NavigateScheduleCommand}" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="CalendarMonthOutline" VerticalAlignment="Center"/>Planning</Hyperlink></TextBlock>
                <TextBlock><Hyperlink Command="{Binding }" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="Camping" VerticalAlignment="Center"/>Festival beheer</Hyperlink></TextBlock>
                <TextBlock><Hyperlink Command="{Binding }" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="CityVariant" VerticalAlignment="Center"/>Gemeentes</Hyperlink></TextBlock>
                <TextBlock><Hyperlink Command="{Binding }" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="AccountGroup" VerticalAlignment="Center"/>Inspecteurs overzicht</Hyperlink></TextBlock>
                <TextBlock><Hyperlink Command="{Binding }" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="FileDocument" VerticalAlignment="Center"/>Offerte overzicht</Hyperlink></TextBlock>
                <TextBlock><Hyperlink Command="{Binding }" TextDecorations="None" Foreground="White"><materialDesign:PackIcon Height="25" Width="25" Margin="0,0,10,0" Kind="MonitorDashboard" VerticalAlignment="Center"/>Dahboard</Hyperlink></TextBlock>
                 </ListBox>
            </Border>

        <Frame Content="{Binding FrameContent}" NavigationUIVisibility="Hidden"></Frame>
    </StackPanel>
</Window>
